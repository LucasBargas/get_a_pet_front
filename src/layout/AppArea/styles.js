import styled from 'styled-components';

const AppAreaContainer = styled.section`
  display: flex;
  flex-direction: column;
  min-height: calc(100vh + 1rem);
  padding-top: 5.25rem;
`;

export default AppAreaContainer;
